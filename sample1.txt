main () {
    print fat(10)[0];
    a = 2 * 4;
}
fat (num :: Int ): Int {
    if (num < 1)
        return 1 ;
    else
        return num * num;
}
divmod(num :: Int, div :: Int): Int , Int {
    q = num / div;
    r = num % div;
    return q, r;
}